#summary Instructions standard à implémenter
#labels Phase-Design

 * `afficher(chaîne, entier, …)`
 * `imprimer(chaîne, entier, …)`
 * `lireFichier(chaîne nomFichier)`

 * `var saisir()`

 * `attendre(entier nombreSecondes)`
 * `pause()`
